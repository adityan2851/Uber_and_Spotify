import 'package:flutter/material.dart';

class Divide extends StatelessWidget {
  // const Divide({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Divider(
      height: 1.0,color: Colors.amberAccent, thickness: 1.0
    );
  }
}
